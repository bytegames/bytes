export enum GameKey {
    UP = 38,
    DOWN = 40,
    LEFT = 37,
	RIGHT = 39,
	SPACEBAR = 32
}

export enum ScreenEdge {
	NORTH,
	SOUTH,
	EAST,
	WEST
}    

export enum Direction {
	UP,
	DOWN,
	LEFT,
	RIGHT,
	NONE
}

export enum Speed {
	SLOW,
	NORMAL,
	FAST
}