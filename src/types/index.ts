import { GameKey, ScreenEdge, Direction, Speed } from './enums.js'
import { IGameObject, IPlayerObject } from './gameobjects.js' 
import { ClockTick, ClockType, Timer } from './timer.js' 
import { Position } from './position.js'

export { 
	GameKey, ScreenEdge, 
	Direction, Speed, Position, 
	IGameObject, IPlayerObject, 
	ClockTick, ClockType, Timer 
}